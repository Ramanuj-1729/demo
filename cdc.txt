The CDC captures two rows when an update command is issued. But suppose the table contains five columns. It won't specify which particular field has been changed. In that scenario, in order to determine which field has been altered, we must compare every single field.